# Legacy Commands

This folder is kept as a reference only. The official Claude plugin standard uses `skills/<name>/SKILL.md` for all user-invoked and model-invoked skills.

The active skill definitions live in `skills/`. Do not add new commands here.
